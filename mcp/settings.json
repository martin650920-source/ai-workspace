{
  "_comment": "MCP 架構設定 — 只引用環境變數，不含實際憑證值。實際值放 ~/.mcp.env（不進 Git）。",
  "mcpServers": {
    "gitlab": {
      "command": "mcp-gitlab",
      "args": ["--url", "https://gitlab.your-company.com"],
      "env": {
        "GITLAB_TOKEN": "${GITLAB_TOKEN}"
      }
    }
  }
}
