# ═══════════════════════════════════════════════════════
# ai-workspace .gitignore
# 策略：骨架與範本進 Git，私密內容留本機 + Google Drive 同步
# ═══════════════════════════════════════════════════════

# ── 個人真實值覆蓋（若日後拆分出來）──────────────────
/rules/global.local.md

# ── 工作專案（含公司 IP）─────────────────────────────
# 白名單機制：projects/ 下預設全部視為機密不進 Git，
# 只有明確列在下面 ! 的檔案才會被公開。
# 好處：改專案代號 / 新增專案都不會漏掉，不用手動維護黑名單。
/projects/*
!/projects/_template.md
!/projects/google-drive-sync.md

# ── 機密（API keys / tokens）─────────────────────────
# 真實憑證放使用者家目錄 ~/.mcp.env，不進這個 repo，
# 這裡只保留 mcp/.env.example 範本（見下方 !）

# ── 知識庫（第二階段，含個人知識）───────────────────
/wiki/
/raw/

# ── 系統 / 工具 ─────────────────────────────────────
.DS_Store
*.swp
*~
.obsidian/
Thumbs.db
