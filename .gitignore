# ═══════════════════════════════════════════════════════
# ai-workspace .gitignore
# 策略：骨架與範本進 Git，私密內容留本機 + Google Drive 同步
# ═══════════════════════════════════════════════════════

# ── 個人真實值覆蓋（若日後拆分出來）──────────────────
/rules/global.local.md

# ── 工作專案 ──────────────────────────────────────────
# （2026-07-02 移除白名單限制，projects/*.md 恢復可進 git 版控，
#  依 ai-workspace-design.md 原始設計：單一事實來源 + symlink 多主機同步。
#  風險自負：repo 目前為 Public，內容機密性由使用者自行把關。）

# ── 機密（API keys / tokens）─────────────────────────
# 真實憑證放使用者家目錄 ~/.mcp.env，不進這個 repo，
# 這裡只保留 mcp/.env.example 範本（見下方 !）

# ── 知識庫（第二階段，含個人知識）───────────────────
/wiki/
/raw/

# ── 系統 / 工具 ─────────────────────────────────────
.DS_Store
*.swp
*~
.obsidian/
Thumbs.db
